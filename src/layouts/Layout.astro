---
import Header from "@/components/Header.astro"

interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<div class="min-h-[90svh] pt-14">
  <Header />
  <div class:list={["max-w-screen-sm mx-auto p-4", className]}>
    <slot />
  </div>
</div>
